pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{js,ts,jsx,tsx}'
      run: bunx eslint {staged_files}
    types:
      glob: '*.{ts,tsx}'
      run: bunx tsc-files {staged_files}
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: bunx commitlint --edit
